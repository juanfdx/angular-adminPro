import { ClickInsideDirective } from './click-inside.directive';

describe('ClickInsideDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickInsideDirective();
    expect(directive).toBeTruthy();
  });
});
